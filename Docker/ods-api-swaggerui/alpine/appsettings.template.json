{
  "WebApiVersionUrl": "${VERSION_URL}",
  "EnableOneRoster": "${ENABLE_ONEROSTER}",
  "OneRosterVersionUrl": "${ONEROSTER_VERSION_URL}",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "SwaggerUIOptions": {
    "OAuthConfigObject": {
      "ClientSecret": "${SAMPLE_SECRET}",
      "ClientId": "${SAMPLE_KEY}"
    }
  }
}
